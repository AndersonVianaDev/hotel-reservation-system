version: "3.8"

services:
  postgres-db:
    image: postgres
    container_name: postgres-db
    environment:
      - POSTGRES_PASSWORD=pg1234
      - POSTGRES_USERNAME=postgres
      - POSTGRES_DB=postgres-db
    ports:
      - 5432:5432